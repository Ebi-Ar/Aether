import { EditorElement } from "./editor/store";

export const SITE_DATA: {
    elements: EditorElement[];
    bg: string;
    height: number;
} = {
    elements: [],
    bg: '#ffffff',
    height: 800
};
